<%# Partial : barre de navigation bottom, inclus dans application.html.erb pour apparaître sur toutes les pages de l'app %>
<div data-controller="whatsapp-modal">
  <%= render "shared/whatsapp_modal" %>

  <nav class="bottom-nav">
    <!-- BOUTON ACCUEIL -->
    <%= link_to root_path, class: "bottom-nav__item #{current_page?(root_path) ? 'bottom-nav__item--active' : ''}" do %>
      <i class="fa-solid fa-house bottom-nav__icon" aria-hidden="true"></i>
      <span>Accueil</span>
    <% end %>

    <!-- SEPARATEUR -->
    <div class="bottom-nav__separator"></div>

    <!-- BOUTON AGENDA -->
    <%= link_to participations_path, class: "bottom-nav__item #{current_page?(participations_path) ? 'bottom-nav__item--active' : ''}" do %>
      <i class="fa-solid fa-calendar-days bottom-nav__icon" aria-hidden="true"></i>
      <span>Agenda</span>
    <% end %>

    <!-- SEPARATEUR -->
    <div class="bottom-nav__separator"></div>

    <!-- BOUTON ABSENCE -->
    <%= link_to new_unavailability_path, class: "bottom-nav__item #{current_page?(new_unavailability_path) ? 'bottom-nav__item--active' : ''}" do %>
      <i class="fa-solid fa-calendar-xmark bottom-nav__icon" aria-hidden="true"></i>
      <span>Absence</span>
    <% end %>

    <!-- SEPARATEUR -->
    <div class="bottom-nav__separator"></div>

    <!-- BOUTON MESSAGES -->
    <button class="bottom-nav__item" data-action="click->whatsapp-modal#open">
      <i class="fa-solid fa-message bottom-nav__icon" aria-hidden="true"></i>
      <span>Messages</span>
    </button>

  </nav>
</div>
